{
  "name": "webpage-content-extractor",
  "version": "1.0.0",
  "description": "Browser extension to extract webpage content for AI chatbots",
  "scripts": {
    "build": "node build.js chrome",
    "build:chrome": "node build.js chrome",
    "build:firefox": "node build.js firefox",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "esbuild": "^0.27.2",
    "fs-extra": "^11.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.44.1"
  }
}